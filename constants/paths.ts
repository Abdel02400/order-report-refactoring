import * as path from 'path';

export const EXPECTED_REPORT = path.join(process.cwd(), 'legacy', 'expected', 'report.txt');
export const OUTPUT_JSON = path.join(process.cwd(), 'legacy', 'output.json');
