export const MORNING_BONUS_HOUR = 10;
export const MORNING_BONUS_RATE = 0.03;
export const NO_MORNING_BONUS = 0;

/** Represents 100% of the price, used as the reference when applying a percentage discount (e.g. `FULL_PRICE_FACTOR - 0.1` = 90%). */
export const FULL_PRICE_FACTOR = 1;

/** Divisor used to convert a percentage value (e.g. 10) into its decimal form (0.10). */
export const PERCENT_BASE = 100;

export const NO_DISCOUNT_RATE = 0;
export const NO_FIXED_DISCOUNT = 0;
